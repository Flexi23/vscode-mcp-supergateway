# Copy to .env and adjust. On Docker Desktop (Windows/macOS), host.docker.internal
# resolves to the host machine, so a host-side LM Studio instance stays reachable.
LMSTUDIO_BASE_URL=http://host.docker.internal:1234/v1

# GitLab PAT for the gitlab upstream inside the `gateway` service (see docker-compose.yml).
GITLAB_PERSONAL_ACCESS_TOKEN=

# SiYuan Note (see docker-compose.yml `siyuan` service). SIYUAN_TOKEN comes from
# the running SiYuan instance: Settings -> About -> API Token. SIYUAN_ACCESS_AUTH_CODE
# is the lock-screen password the siyuan-note upstream doesn't need but the container requires.
SIYUAN_TOKEN=
SIYUAN_ACCESS_AUTH_CODE=
