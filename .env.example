# Copy to .env and adjust. On Docker Desktop (Windows/macOS), host.docker.internal
# resolves to the host machine, so a host-side LM Studio instance stays reachable.
LMSTUDIO_BASE_URL=http://host.docker.internal:1234/v1

# GitLab PAT for the gitlab upstream inside the `gateway` service (see docker-compose.yml).
GITLAB_PERSONAL_ACCESS_TOKEN=

# SiYuan Note (see docker-compose.yml `siyuan` service). For local/dev installs,
# the SiYuan kernel can run without a token. Leave SIYUAN_TOKEN empty and set
# SIYUAN_TOKEN_REQUIRED=false to disable auth entirely. SIYUAN_ACCESS_AUTH_CODE is
# the lock-screen password required by the container's own web UI.
# If you do want an API token, set it here and set SIYUAN_TOKEN_REQUIRED=true.
SIYUAN_TOKEN=
SIYUAN_TOKEN_REQUIRED=false
SIYUAN_ACCESS_AUTH_CODE=siyuan_access_auth_code_here

# Absolute path to the host workspace root (e.g. the outer monorepo checkout).
# The SiYuan workspace dir (notebooks/docs/assets) and the codebase-memory
# auto-index target are both derived from this single root — set only this.
WORKSPACE_ROOT=/absolute/path/to/workspace-root
